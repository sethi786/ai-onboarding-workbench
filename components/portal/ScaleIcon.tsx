import { Scale } from 'lucide-react';

/** Regulatory mapping mark. Wrapped so the icon choice lives in one place. */
export function ScaleIcon({ className }: { className?: string }) {
  return <Scale className={className} />;
}
